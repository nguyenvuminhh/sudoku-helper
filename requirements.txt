fastapi>=0.135,<1
uvicorn[standard]>=0.42,<1
python-multipart>=0.0.22,<1

# Optional local OCR path used by /api/sudoku/ocr when installed.
# The system tesseract binary is also required for real digit recognition.
pillow>=12,<13
pytesseract>=0.3.13,<1
